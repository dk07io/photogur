<h1>Hi my first picture page!</h1>

<ul>
  <% @most_recent_pictures.each do |picture| %>
    <li><%= picture.title %>, <%= picture.artist %></li>
    <%= link_to image_tag(picture.url), "/pictures/#{picture.id}" %>
  <% end %>
</ul>


<%= link_to "Back to Pictures", pictures_url %>


